#ifndef PROFILES_H
#define PROFILES_H

#include <iostream>
#include <fstream>
#include <string>

// Function declaration
void handlePlayerNames();
void chooseLevel(std::string name);
void randomLoad(int level);

//void chooseLevel(std::string name);

#endif 